# Base URL of the Activity Ranking API (the system under test).
# The SUT is started by the Cucumber hooks for the in-process BDD run.
SUT_BASE_URL=http://localhost:3000

# Open-Meteo dependency URLs used by the SUT in production.
OPEN_METEO_GEOCODING_URL=https://geocoding-api.open-meteo.com/v1/search
OPEN_METEO_FORECAST_URL=https://api.open-meteo.com/v1/forecast
